@*model jaj.Models.video *@
@model List<jaj.Models.video>


@{
    ViewBag.Title = "Home Page";
}

<h4>Newest videos</h4>

<div class="row">
    @for (int i = Model.Count()-1; i > Model.Count()-6; i--)
    {
        <div class="col-md">
            <a href="/Upload/videoPage/@Model[i].videoID">
                <video width="150" height="100">
                    <source src="@Url.Content(Model[i].videoPath)" />
                    <video width="150" height="100" poster="@Url.Content(Model[i].videoPath)">
                        <source src="@Url.Content(Model[i].videoPath)" />
                    </video>
                </video>
                <br />
                @(Model[i].videoName)
            </a>
        </div>
    }
</div>

<br />
<br />
<br />
<br />


    <div class="row">
        <div class="form-group">
            @{
                @*
                 Tu dodati HomeTag = loggedinUser.FavTag
                *@
            string HomeTag = "Gaming";
            if (User.Identity.IsAuthenticated)
            {
                HomeTag = ViewData["taggovi"].ToString();
               }
            }
            @*
        @Html.DropDownList(HomeTag, jaj.Models.TagList.GetTagList())
            *@


            Newest video in @HomeTag tag
        </div>
    </div>


<div class="row">
    @{

        for (int i = 0; i < Model.Count(); i++)
        {
            if (Model[i].tag == HomeTag)
            {

                <div class="col-md">
                    <a href="/Upload/videoPage/@Model[i].videoID">
                        <video width="150" height="100">
                            <source src="@Url.Content(Model[i].videoPath)" />
                            <video width="150" height="100" poster="@Url.Content(Model[i].videoPath)">
                                <source src="@Url.Content(Model[i].videoPath)" />
                            </video>
                        </video>
                        <br />
                        @(Model[i].videoName)
                    </a>

                </div>
            }

        }

    }
</div>


